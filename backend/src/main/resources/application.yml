server:
  port: ${PORT:8080}

spring:
  application:
    name: sensory-aware-backend
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  allowed-origins: ${APP_ALLOWED_ORIGINS:http://localhost:5173}
  demo-mode: ${APP_DEMO_MODE:true}
