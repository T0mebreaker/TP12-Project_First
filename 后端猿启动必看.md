后端已经通过maven打包成功，电脑只需要安装 **JDK 21** 即可启动。

在后端根目录下打开cmd，使用：

```
.\mvnw.cmd spring-boot:run
```

即可启动后端服务

然后在任意网页输入：

```
http://localhost:8080/api/health
```

若出现：

```
{
  "service": "sensory-aware-backend",
  "status": "UP"
}
```

即可确定后端服务已经启动

所以完整关系是：

```
GitHub clone
   ↓
backend
.\mvnw.cmd spring-boot:run
   ↓
localhost:8080

另一个终端
   ↓
frontend
npm install
npm run dev
   ↓
localhost:5173
   ↓ Axios
localhost:8080
```

前端访问：

```
http://localhost:5173
```

